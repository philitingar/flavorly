//
//  Color.swift
//  Flavourly
//
//  Created by Timea Bartha on 15/5/25.
//

import SwiftUI

extension Color {
    // Dark Fantasy Forest Color Palette
    static let darkForestGreen = Color(red: 27 / 255, green: 43 / 255, blue: 14 / 255) // Dark Forest Green
    static let softGlowGreen = Color(red: 224 / 255, green: 248 / 255, blue: 216 / 255) // Soft Glow (Light Green)
    static let mutedLeafGreen = Color(red: 162 / 255, green: 200 / 255, blue: 160 / 255) // Muted Leaf Green
    static let goldenYellow = Color(red: 255 / 255, green: 215 / 255, blue: 0 / 255) // Golden Yellow
    static let softAquamarine = Color(red: 168 / 255, green: 225 / 255, blue: 212 / 255) // Soft Aquamarine
    static let glowingInsectGreen = Color(red: 170 / 255, green: 255 / 255, blue: 0 / 255) // Bright Green
    static let coral = Color(red: 255 / 255, green: 127 / 255, blue: 80 / 255) // Coral
    static let deepRed = Color(red: 255 / 255, green: 69 / 255, blue: 0 / 255) // Deep Red
    static let lightGray = Color(red: 204 / 255, green: 204 / 255, blue: 204 / 255) // Light Gray
    static let darkGray = Color(red: 68 / 255, green: 68 / 255, blue: 68 / 255) // Dark Gray
    static let veryDarkGreen = Color(red: 0 / 255, green: 51 / 255, blue: 0 / 255) // Very Dark Green/Black
    static let forestGreenDivider = Color(red: 91 / 255, green: 140 / 255, blue: 42 / 255) // Divider Color
    
    // Fantasy Baby Green Color Palette
    static let fantasyBabyGreenBackground = Color(red: 187 / 255, green: 222 / 255, blue: 140 / 255) // Light Baby Green
       static let sparklingMint = Color(red: 0 / 255, green: 128 / 255, blue: 128 / 255) // Darker Mint
       static let fairyDustYellow = Color(red: 255 / 255, green: 215 / 255, blue: 0 / 255) // Soft Yellow for highlights
       static let magicalLavender = Color(red: 128 / 255, green: 0 / 255, blue: 128 / 255) // Darker Lavender
       static let enchantedPink = Color(red: 255 / 255, green: 105 / 255, blue: 180 / 255) // Rich Pink
       static let emeraldAccent = Color(red: 0 / 255, green: 128 / 255, blue: 0 / 255) // Darker Emerald Green
       static let softPeach = Color(red: 255 / 255, green: 160 / 255, blue: 122 / 255) // Darker Peach
       static let calmingCyan = Color(red: 0 / 255, green: 191 / 255, blue: 255 / 255) // Darker Calm Blue
       static let twilightPurple = Color(red: 75 / 255, green: 0 / 255, blue: 130 / 255) // Darker Twilight Purple
       static let mistyGray = Color(red: 169 / 255, green: 169 / 255, blue: 169 / 255) // Medium Gray
    // Pink Bubble Gum Color Palette
        static let bubbleGumBackground = Color(red: 255 / 255, green: 182 / 255, blue: 193 / 255) // Baby Pink
        static let darkBubbleGum = Color(red: 220 / 255, green: 100 / 255, blue: 130 / 255) // Dark Bubble Gum Pink
        static let darkRose = Color(red: 165 / 255, green: 43 / 255, blue: 65 / 255) // Dark Rose
        static let sweetCandiedPink = Color(red: 255 / 255, green: 105 / 255, blue: 180 / 255) // Sweet Pink
        static let raspberryRed = Color(red: 227 / 255, green: 11 / 255, blue: 93 / 255) // Raspberry Red
        static let cottonCandyBlue = Color(red: 174 / 255, green: 239 / 255, blue: 255 / 255) // Light Cotton Candy Blue
        static let lavenderPurple = Color(red: 150 / 255, green: 123 / 255, blue: 182 / 255) // Lavender Purple
        static let softGray = Color(red: 211 / 255, green: 211 / 255, blue: 211 / 255) // Soft Gray for text fields and dividers
    // Dark Pink Neon Color Palette
        static let neonBlack = Color(red: 0 / 255, green: 0 / 255, blue: 0 / 255) // Black Background
        static let neonPink = Color(red: 255 / 255, green: 20 / 255, blue: 147 / 255) // Neon Pink
        static let brightNeonMagenta = Color(red: 255 / 255, green: 0 / 255, blue: 255 / 255) // Bright Neon Magenta
        static let vibrantFuchsia = Color(red: 255 / 255, green: 105 / 255, blue: 180 / 255) // Vibrant Fuchsia
        static let electricBlue = Color(red: 0 / 255, green: 191 / 255, blue: 255 / 255) // Electric Blue for contrast
        static let neonPurple = Color(red: 128 / 255, green: 0 / 255, blue: 128 / 255) // Neon Purple
        static let softGrayish = Color(red: 211 / 255, green: 211 / 255, blue: 211 / 255) // Soft Gray for text fields
    // Frosty Blue Color Palette
        static let frostyBlueBackground = Color(red: 173 / 255, green: 216 / 255, blue: 230 / 255) // Light Frosty Blue
        static let glacierBlue = Color(red: 0 / 255, green: 150 / 255, blue: 220 / 255) // Glacier Blue
        static let icyTurquoise = Color(red: 0 / 255, green: 128 / 255, blue: 128 / 255) // Icy Turquoise
        static let arcticWhite = Color(red: 255 / 255, green: 255 / 255, blue: 255 / 255) // Arctic White
        static let deepIceBlue = Color(red: 70 / 255, green: 130 / 255, blue: 180 / 255) // Deep Ice Blue
        static let ceruleanBlue = Color(red: 0 / 255, green: 123 / 255, blue: 167 / 255) // Cerulean Blue
        static let paleAqua = Color(red: 0 / 255, green: 139 / 255, blue: 139 / 255) // Pale Aqua
        static let frostGray = Color(red: 220 / 255, green: 220 / 255, blue: 220 / 255) // Frost Gray for dividers
    // Neon Blue Color Palette
        static let neonBlue = Color(red: 0 / 255, green: 153 / 255, blue: 255 / 255) // Neon Blue
        static let brightNeonCyan = Color(red: 0 / 255, green: 255 / 255, blue: 255 / 255) // Bright Neon Cyan
        static let vividSkyBlue = Color(red: 135 / 255, green: 206 / 255, blue: 250 / 255) // Vivid Sky Blue
        static let neonTurquoise = Color(red: 0 / 255, green: 255 / 255, blue: 218 / 255) // Neon Turquoise
        static let brightBlueViolet = Color(red: 138 / 255, green: 43 / 255, blue: 226 / 255) // Bright Blue Violet
}
